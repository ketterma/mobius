apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: openebs-zfs-ai
provisioner: zfs.csi.openebs.io
allowVolumeExpansion: true
reclaimPolicy: Delete
parameters:
  poolname: "ai/CLUSTER_NAME"  # Will be patched per cluster
  fstype: "zfs"
  compression: "lz4"
  dedup: "off"
  thinprovision: "yes"
