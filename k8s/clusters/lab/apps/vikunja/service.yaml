apiVersion: v1
kind: Service
metadata:
  name: vikunja
  namespace: vikunja
spec:
  selector:
    app.kubernetes.io/name: vikunja
    app.kubernetes.io/instance: vikunja
  ports:
    - name: http
      port: 80
      targetPort: http
