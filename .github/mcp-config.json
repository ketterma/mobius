{
  "mcpServers": {
    "hass-mcp": {
      "command": "uvx",
      "args": ["hass-mcp"],
      "env": {
        "HA_URL": "https://ha.jaxonk.com",
        "HA_TOKEN": "${HA_TOKEN}"
      }
    }
  }
}